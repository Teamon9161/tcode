[permissions]
allow = ["run(cargo fmt --check; cargo check --workspace)", "run(cargo fmt; cargo test -p tcode-core -p tcode-tools --test agent_loop)", "run(cargo fmt; cargo test -p tcode-tools --test agent_loop plan_mode_allows_scratch_writes_but_not_project_writes -- --exact; cargo test -p tcode-tui live_panel::tests::task_tree_renders_the_agent_kind_as_context_not_a_green_status -- --exact)", "run(cargo fmt --check; if ($LASTEXITCODE -ne 0) { cargo fmt; if ($LASTEXITCODE -ne 0) { exit $LASTEXITCODE } }; cargo test -p tcode-tui; if ($LASTEXITCODE -ne 0) { exit $LASTEXITCODE }; git diff --check; if ($LASTEXITCODE -ne 0) { exit $LASTEXITCODE }; git status --short)", "web_search", "run(cargo fmt && cargo test -p tcode-providers codex)", "run(cargo fmt --check && cargo test -p tcode-tools --test agent_loop auto_mode_classifier_outages_pause_and_notify_the_frontend && cargo check -p tcode)", "run(cargo fmt; cargo test -p tcode-core auto_mode)"]
