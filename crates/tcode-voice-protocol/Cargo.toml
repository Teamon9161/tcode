[package]
name = "tcode-voice-protocol"
version = "0.1.16"
edition = "2021"
description = "Shared release and wire compatibility metadata for tcode voice"
license = "Apache-2.0"

[lib]
path = "src/lib.rs"
